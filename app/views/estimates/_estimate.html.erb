<li data-id="<%= estimate.id %>">
  <%= link_to estimate.title, estimate_path(estimate) %>
  <%= link_to "Delete", estimate_path(estimate), :method => :delete, :confirm => "Are you sure?", :remote => true %>
</li>
